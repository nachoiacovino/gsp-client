import React from 'react'
import './Button.css'

const Button = ({ character }) => {
    return (
        <div className="Button">
            {character}
        </div>
    )
}

export default Button
